/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com'], // Add the hostname for placeholder images
    // If you have your own image host, add it here, e.g., 'your-image-host.com'
  },
};

module.exports = nextConfig;